# 实施模式回答：项目名重命名（今晚吃什么 → 今天吃什么）

> 项目中文名：**今天吃什么** ｜ 仓库：`ll96victor/What-should-we-eat-today`
> Skill：my-coding-helper **v3.1.25-verified**
> 依据基线：`da7dd71`

---

## 1. 文件阶段声明

本文件是**实施模式**的产出。

- 本轮把项目中文名由旧名「今晚吃什么」统一为 **今天吃什么**。
- **纯文案替换，无逻辑变更**：不入库任何功能改动，不改数据结构，不改部署配置。
- 用户额外确认：正文中的自然语句（「不知道今晚吃什么」这类）也一并改为「今天」。

---

## 2. 任务等级与模式

| 项 | 值 |
|---|---|
| 任务等级 | **L2**（跨 9 个文件的纯文案替换；无逻辑、无数据、无接口变化） |
| 模式 | 实施模式 |
| 风险等级 | 中低。风险不在逻辑，而在**中文批量替换的编码事故**与**误伤自然语句** |

---

## 3. 用户确认的边界

| 问题 | 用户选择 |
|---|---|
| 品牌名（页面标题/页头/README 标题等）改不改 | ✅ 改 |
| 自然语句（「不知道今晚吃什么」「10 秒内决定今晚吃什么」）改不改 | ✅ **一起改成「今天」** |

**未改、且用户未要求改的部分**：

| 项 | 为什么不改 |
|---|---|
| 仓库名 `What-should-we-eat-today` | 改了会断掉 GitHub Pages 与 CloudBase 的既有地址 |
| GitHub Pages / CloudBase 线上地址 | 同上 |
| CloudBase 环境名与环境 ID | 与项目显示名无关 |
| 英文名 `What should we eat today?` | 本来就是 today，无需改 |
| `localStorage` 键 `what-should-we-eat-today.settings` | 改了会让所有老用户的数据失联 |
| `tasks/todo.md` 与 `answers/` 中的旧名 | 那是**历史轮次的过程记录**，改写等于伪造记录（见 §7） |

---

## 4. 修改的文件

**共 9 个文件、18 行**（改动前逐个备份到 `history/*.before-20260923-v6`）。

| 文件 | 处数 | 具体位置 |
|---|---:|---|
| `index.html` | 3 | `<title>`、`<h1 class="hero-title">`、`<meta name="description">` |
| `app.js` | 3 | 文件头注释、`console.warn` 前缀、`console.error` 前缀 |
| `README.md` | 4 | 标题、问题陈述、目标句、甜品说明里的「今晚的素菜」 |
| `specs.md` | 3 | §1 问题陈述、§1 成功的样子、§4 界面示意；**并删除**头部那条「尚未执行的重命名」说明 |
| `assets/favicon.svg` | 1 | `aria-label`（无障碍朗读的项目名） |
| `styles.css` | 1 | 文件头注释 |
| `project-achievements.md` | 1 | 成果条目标题 |
| `tools/serve.mjs` | 1 | 启动日志 `今晚吃什么 · 本地服务已启动` |
| `tools/build-recipes.mjs` | 1 | 注释里的「今晚的素菜」 |

**先删后换的说明**：`specs.md` 头部的「关于项目名：……这是尚未执行的重命名」整段
在替换**之前**先删除。否则批量替换会把该段里的旧名也换成新名，
得到「当前代码里显示的仍是旧名『今天吃什么』」这种自相矛盾的句子。

### 未修改的文件

| 文件 | 为什么没改 |
|---|---|
| `nutrition.js` / `data/*.json` | 不含项目名 |
| `tools/build-foods.mjs` / `check-*.mjs` / `deploy-cloudbase.mjs` | 不含项目名 |
| `canvas/` | 平台生成文件，按要求不处理 |

---

## 5. 执行方式（按 Skill 的中文批量替换安全规范）

该规范来自一次真实事故（PowerShell 5.1 默认编码把中文 Obsidian 文档改乱码），
本轮逐条遵守：

| 规范要求 | 本轮做法 |
|---|---|
| 不用 PowerShell 默认编码做中文批量替换 | ✅ 全程用 Node，`readFileSync(f,'utf8')` + `writeFileSync(f,s,'utf8')` 显式 UTF-8 |
| 替换前先备份 | ✅ 7 个待改文件备份到 `history/*.before-20260923-v6` |
| 替换前跑空跑命中的清单 | ✅ 先输出「18 行 / 9 文件」逐条 before→after 对照，确认无误后才落盘 |
| 替换后抽样检查中文 | ✅ 抽查 index.html / app.js / README / favicon 关键行，中文完好 |
| 替换后扫描乱码标记 | ✅ 扫 `浠诲姟` `闂` `缁存姢` `鏂囨` `鎸囨淳` `宸ュ崟` `霢` `æ` `è` `ç` `�`，9 个文件**全部未命中** |
| 旧串残留与乱码**两者都扫过**才算完成 | ✅ 两项均已扫描，结果见 §6 |

**实现细节**：用的是整文件字符串 replace（不是按行 split/join），
因此原有换行符（CRLF/LF）不被改动。

---

## 6. 验证情况

### 6.1 文本层

| 检查项 | 结果 |
|---|---|
| 旧名「今晚吃什么」残留 | ✅ 全仓仅剩 `tasks/todo.md:51` 一处，为**刻意保留的历史记录**（见 §7） |
| 「今晚的素菜」残留 | ✅ 0 |
| 乱码标记扫描 | ✅ 9 个文件全部未命中 |
| 中文抽样 | ✅ 抽查行中文完好 |

### 6.2 代码层（确认改名没碰坏任何东西）

| 检查项 | 结果 |
|---|---|
| `app.js` 按 ES Module 解析 | ✅ `node --check` 通过 |
| `tools/serve.mjs` / `tools/build-recipes.mjs` 语法 | ✅ 均通过 |
| 营养语义自检 | ✅ `check-nutrition-semantics.mjs` 仍 **72 / 72** 通过 |
| 逻辑改动 | ✅ 零——3 处 app.js 改动全在注释与日志前缀，不参与任何计算 |

### 6.3 真实页面验证（浏览器实测）

| 检查项 | 实测 |
|---|---|
| 标签页标题 | ✅ `今天吃什么` |
| 页头 h1 | ✅ `今天吃什么` |
| meta description | ✅ `今天吃什么：荤素搭配的随机家常菜单…` |
| 菜单是否仍正常 | ✅ 8 张卡片正常渲染 |
| 本地服务启动日志 | ✅ `今天吃什么 · 本地服务已启动` |

> 过程中有一处**我自己的验证脚本出错**，如实记录：
> 用正则剥离多行 `import` 语句来检查 app.js 语法，把代码切坏了，
> 误报「语法错误」。改用 `node --check` 后确认文件本身正确。
> 这是检查方式的问题，不是文件的问题。

---

## 7. 刻意未改的一处（重要）

`tasks/todo.md:51` 仍写着：

> - 项目中文名「今晚吃什么」未改（用户本轮明确禁止顺手改）

这是**营养语义重构那一轮**的任务单记录，描述的是**那一轮的边界**。
它当时是准确的，现在也是准确的历史陈述。改写它等于伪造过程记录。
同理，`answers/` 下 8 份历史回答文件里的旧名也**一律未动**——
那些是「当时发生了什么」的档案，不属于本次重命名的作用范围。

如果以后希望仓库里零残留，需要单独决定「是否要改写历史记录」，
本轮不擅自做这个决定。

---

## 8. 版本变化

**本项目不使用版本号字段**（纯静态站点，无 `package.json`、无 userscript 元数据块）。

---

## 9. 维护文档更新摘要

| 文档 | 更新内容 |
|---|---|
| `specs.md` | §1、§4 文案随改名更新；**删除**头部「尚未执行的重命名」说明 |
| `README.md` | 标题与正文文案随改名更新 |
| `project-achievements.md` | 成果条目标题更新（按 §11.4，重命名本身**不作为新成果登记**） |
| 本文件 | 本轮过程记录 |

---

## 10. 未验证风险

| # | 风险 | 说明 |
|---|---|---|
| 1 | **线上仍是旧名** | 本地改名完成，但 GitHub Pages 与 CloudBase 上的站点在推送前仍是旧名。Pages 会在 push 后自动重建；**CloudBase 需要手动重跑部署命令**（见 §11） |
| 2 | 浏览器缓存 | 标题与页头是 HTML 内容，浏览器可能缓存旧页面；实机复核时需硬刷新 |
| 3 | 无障碍标签的生效范围 | favicon 的 `aria-label` 已改，但屏幕阅读器实际朗读效果未实测 |
| 4 | 已分享出去的链接标题 | 若此前有社交平台抓取过旧标题，那些快照不会自动更新，无法处理 |

---

## 11. 后续建议

1. **让线上同步新名字**：
   - GitHub Pages：`git push` 后自动重建
   - CloudBase：需**手动再跑一次** `node tools/deploy-cloudbase.mjs what-should-we-eat-d0cym0b8ea64c`
   （`dist/` 已在本地重新生成，内容已是新名）
2. `tasks/todo.md` 与 `answers/` 中的旧名保持原样；若希望清理，单独决定。
3. 仓库名与线上地址保持不变，这是刻意的，避免断链。

---

## 12. 完成审计

| 检查项 | 结果 |
|---|---|
| 是否只改了文案 | ✅ 是——没有任何逻辑、数据、接口改动 |
| 是否备份后再改 | ✅ `history/*.before-20260923-v6`（7 个文件） |
| 是否先空跑再落盘 | ✅ 18 行逐条对照确认后才执行 |
| 旧串残留与乱码是否**两者都扫过** | ✅ §6.1 |
| 是否做了真实页面验证 | ✅ §6.3 |
| 是否误伤自然语句之外的内容 | ✅ 无（逐行对照过） |
| 是否伪改了历史记录 | ✅ 未动 `answers/` 与本轮以外的 `tasks/` 记录 |
| 是否如实记录自身失误 | ✅ §6.3 记录了验证脚本的误报 |

---

*实施完成时间：2026-09-23 18:14 ｜ 依据基线：`da7dd71`*
