# 项目成果记录

> 记录**已完成且值得长期保存的成果**。
> 与其余文档的分工：`specs.md` = 应该做什么；Git 提交 = 改了什么；`answers/` = 这轮怎么做的。
>
> 判定标准：只记有真实业务价值、可被证据支撑的结果。不记笔误、样式微调、例行维护。

---

## 成果 1 · 家庭晚餐决策工具（今天吃什么 V1）

**时间**：2026-09-23
**证据**：提交 `ac51f01` → `742ebbc`；线上 https://ll96victor.github.io/What-should-we-eat-today/

### 问题

每天在家做饭时需要现场决定吃什么，这个决策既耗时又反复发生。

### 方案

一个纯静态的网页工具，打开即给出一桌荤素搭配的菜，不满意就换一组，点开看做法。
无需登录、无需注册、无需后端，全部逻辑在浏览器内运行。

**技术栈**：原生 HTML / CSS / JavaScript + JSON（零框架、零构建、零运行时依赖）

### 关键决策与难点

1. **坚持零依赖与零构建。** 站点就是仓库根目录，同一套代码同时适配 GitHub Pages 与
   腾讯云 CloudBase 静态托管，不维护第二份。
   （2026-09-23 两平台均已完成真实部署并线上验证，这条从设计意图变成了实测事实。）

2. **菜谱数据只取上游的文字。** 上游 HowToCook（Unlicense）371 篇 markdown，
   只提取菜名、食材用量、步骤、小贴士、来源链接。**不采用上游图片**——图片托管在境外 CDN，
   与「国内网络直连可用」的硬约束冲突；实测 370 道菜的图片全部为空时，核心功能完全正常。

3. **`menuRole` 用人工复核表而非目录推断。** 上游按「荤菜 / 素菜」分目录，
   但家常菜里鸡蛋、豆腐、豆类同属蛋白质来源，机械映射会把「西红柿炒鸡蛋」归成素菜。
   为此维护了一张逐条纠正的对照表，并明确了三类**有意排除出随机池**的条目：
   佐餐小食（水煮蛋、温泉蛋）、甜品当素菜（拔丝土豆）、技法演示（水油焖蔬菜）。
   排除标准是「分类错误」，不是个人口味偏好——口味不常见的菜（如印度菜）保留在池内，
   用户点「换一组」即可。

4. **屏蔽关键词只有一个判断入口。** 荤素两池共用同一个 `isBlocked()`；
   匹配范围含菜名、分类、简介、食材、标签，**不含做法步骤**——步骤里常出现无关词，
   算进去会造成大量误伤。

5. **营养数字只来自数据或公式。** 食物营养值取自 USDA FoodData Central
   （**CC0 公有领域**，可自由再分发），构建时下载并固化为本地 JSON，
   运行时零网络请求。中文名到 USDA 记录的对照关系是本项目建立的，但**数值不是手抄的**——
   由构建脚本从原始数据按键取出，每个数字都能回溯到具体那条记录。

6. **营养覆盖率的诚实边界。** 实测食材识别率 80.8%，菜谱层面
   已估算 32.7% / 部分估算 63.2% / 无法估算 4.1%。界面必须区分这三种状态，
   无法估算时**不给任何数字**。认不出的食材一律不猜重量。

7. **修掉一个会让数字离谱的建模问题。** 上游菜谱里「熟猪油 2000 克」是一整锅炸油，
   全额计入会算出单道菜 21037 kcal。改为：超过 150g 的用油判为炸油不计入，
   并在界面明说——宁可略微低估，也不能给出明显错误且看起来精确的数字。

8. **装饰动效不得影响内容可见性。** 验证中发现宿主环境不推进 CSS 动画时钟，
   暴露出真实风险：入场动画若以 `opacity: 0` 起始，一旦被系统中断（省电模式、后台标签页），
   页面会永久空白。改为只做位移不做透明度，最坏情况从「内容全白」降级为「偏移几像素」。

9. **在多个平台实测而非推断。** 对本地服务器、GitHub Pages 子路径、移动视口三种运行形态
   分别做了完整验收，包括 200 次连续换组的约束校验。

10. **附加能力默认关闭，首页只保留核心承诺。** 营养与健康功能（家庭成员、主食、
    本餐营养、健康参考）全部收在一个默认关闭的开关后面。关闭时不仅不显示，
    也**不参与计算**；但数据保留在本地，重新打开即恢复，随机菜单全程不受影响。

11. **重构为多成员模型，并让「人数」只有一个事实源。** 原先只有单份健康资料，
    改为最多 8 位家庭成员（可增删改、可停用），每位**独立**计算 BMI / BMR / TDEE。
    同时消除了潜在的双事实源问题：「家里有谁」由成员列表描述，
    「今天来几个人」由单独字段描述且可为「跟随成员」——因此
    「家庭成员 5 人、今天实际吃 4 人」是合法的，两者不会互相锁死。
    新增与编辑共用同一个弹出表单，没有第二套表单；删除需二次确认且不影响其他成员与其他设置。

12. **纠正营养模块的产品语义，让数字不再被读成别的东西。** 数值本身没有算错，
    错的是它们容易被理解成另一个概念。重构后严格区分三层：
    **每日能量参考**（一整天 24 小时全部摄入的参考量，含早/午/晚及其他）、
    **这桌菜估算**（按菜谱所写用量 + 本次主食准备量算出来的）、
    **实际摄入**（当前版本不知道，也不伪造）。配套三处结构性调整：

    - **每日能量改为每人独立、双来源**：每位成员各有一份，可选「自动估算」或「自己设定」，
      不做成全家共用一个数字；手动值只设技术防呆范围（500–8000 kcal），
      程序不对它做任何健康判断。
    - **碳水/脂肪的每日参考范围以该成员的每日能量参考为基准**（手动值优先），
      消除了「能量设 1800、范围却按自动算出的 2300 算」这类口径冲突。
      自动模式下数值与重构前完全一致，因此可做逐位回归对照。
    - **新增「每天通常吃几餐」（2–6），只记录不计算**：明确拒绝
      「每日能量 ÷ 餐数 = 每餐该吃多少」，也不给 2/4/5/6 餐编造能量比例。

    同时统一用量口径：**菜谱用量 ≠ 实际购买量 ≠ 实际使用量 ≠ 实际摄入量**，
    主食克数一律表述为「本次用于计算的主食准备量」而非「吃掉的量」，
    「人均」一律注明只是数学平均。语义层面的约束被固化成
    `tools/check-nutrition-semantics.mjs` 的 72 条断言，覆盖新老数据兼容、
    防呆边界、除法禁令与旧口径残留扫描。

### 结果

- 线上可用：https://ll96victor.github.io/What-should-we-eat-today/
- 菜谱 370 道；随机池 荤菜 188 / 素菜 40（7520 种搭配）
- 食物营养表 120 条；食材识别率 80.8%
- 首页默认极简；营养与健康为可选的默认关闭模块
- 家庭成员最多 8 位，每人独立计算健康参考，每人各有一份可自动估算或自己设定的每日能量参考
- 营养语义自检 72 条断言全通过；覆盖率自检结果与重构前逐项相同（32.7% / 63.2% / 4.1%）
- 移动端优先，无第三方接口依赖
- 双平台均已上线：GitHub Pages + 腾讯云 CloudBase 静态托管（同一套代码）

### 如实标注的缺口

- **素菜池偏小**：40 道，一周使用遇到重复素菜概率约 99.8%。
  经真实检索，**没有找到第二个「授权清晰 + 含做法步骤 + 可自动整理」的中文菜谱来源**
  （候选逐一核查：无 LICENSE 的排除、仅有菜名无做法的排除、GPL 传染性排除），
  按约束保持现状而非降低质量凑数。
- **默认测试域名带访问提示页**：CloudBase 默认域名每次访问会先显示一个
  「页面访问提示」警告页，需点「确定访问」才进入网站。这是该平台默认域名的固有行为，
  绑定已备案的自定义域名后消失。对追求「打开即用」的体验算是一处折扣，如实记录。
- **用户真实使用验证**：尚未进行。

---

## 协作方式

本项目由用户提出需求与约束、逐轮验收，AI 承担实现、自动验证与文档整理。
开发过程遵循 `my-coding-helper` 工作流。
